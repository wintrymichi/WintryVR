<?xml version="1.0" encoding="utf-8"?>
<!--
  WintryVR Android manifest for Meta Quest 3 / 3S.
  Permissions are requested at runtime, right before the feature that needs them (see PermissionManager).
  horizonos.permission.HEADSET_CAMERA enables the Passthrough Camera API (Horizon OS v74+).
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.wintry.wintryvr"
          android:installLocation="auto">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="horizonos.permission.HEADSET_CAMERA" />
    <uses-permission android:name="com.oculus.permission.HAND_TRACKING" />
    <uses-permission android:name="com.oculus.permission.USE_SCENE" />
    <uses-permission android:name="com.oculus.permission.USE_ANCHOR_API" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <uses-feature android:name="android.hardware.vr.headtracking" android:version="1" android:required="true" />
    <uses-feature android:name="oculus.software.handtracking" android:required="false" />
    <uses-feature android:name="com.oculus.feature.PASSTHROUGH" android:required="true" />
    <uses-feature android:name="com.oculus.experimental.enabled" android:required="false" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
    <uses-feature android:glEsVersion="0x00030002" android:required="true" />

    <!--
      No android:label here on purpose. Unity's generated launcher manifest already sets it to @string/app_name,
      taken from PlayerSettings.productName, and declaring it again in this one makes the merger fail
      :launcher:processReleaseMainManifest with two values it will not choose between — after IL2CPP has already
      compiled, which is a long wait for a duplicated attribute. Rename the app in Player Settings instead.
    -->
    <application android:allowBackup="false" android:usesCleartextTraffic="true">
        <!-- Supported headsets; Quest 3S is covered by the same "quest3" family entry plus explicit listing -->
        <meta-data android:name="com.oculus.supportedDevices" android:value="quest3|quest3s" />
        <meta-data android:name="com.oculus.handtracking.frequency" android:value="HIGH" />
        <meta-data android:name="com.oculus.handtracking.version" android:value="V2.0" />
        <!--
          This must stay UnityPlayerActivity, and the player's Android "Application Entry Point" must be
          Activity (WintryProjectSetup pins it; the setup verifier checks it). Unity 6 defaults to GameActivity,
          which puts only UnityPlayerGameActivity in the APK; Horizon OS launches the activity carrying the
          com.oculus.intent.category.VR category — this one — and a missing class means the app closes before
          it draws anything. In the headset library that looks like "tap the icon, nothing happens".
        -->
        <activity android:name="com.unity3d.player.UnityPlayerActivity"
                  android:theme="@style/UnityThemeSelector"
                  android:screenOrientation="landscape"
                  android:launchMode="singleTask"
                  android:configChanges="mcc|mnc|locale|touchscreen|keyboard|keyboardHidden|navigation|orientation|screenLayout|fontScale|screenSize|smallestScreenSize|uiMode|density"
                  android:resizeableActivity="false"
                  android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="com.oculus.intent.category.VR" />
            </intent-filter>
            <meta-data android:name="unityplayer.UnityActivity" android:value="true" />
            <meta-data android:name="com.oculus.vr.focusaware" android:value="true" />
        </activity>
    </application>
</manifest>
